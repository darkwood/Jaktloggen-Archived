<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:abstractions="clr-namespace:ImageCircle.Forms.Plugin.Abstractions;assembly=ImageCircle.Forms.Plugin.Abstractions"
             x:Class="Jaktloggen.Views.JaktListPage">
  <ListView ItemsSource="{ Binding GroupedItems }"
            IsGroupingEnabled="true"
            GroupDisplayBinding="{ Binding Name }"
            GroupShortNameBinding ="{ Binding ShortName }"
            ItemSelected="ListViewItems_OnItemSelected">    
    <ListView.ItemTemplate>
      <DataTemplate>
        <ViewCell>
          <StackLayout Orientation="Horizontal" Padding="5">
            <abstractions:CircleImage Source="{Binding Image}" BorderThickness="1" BorderColor="White" HeightRequest="40" WidthRequest="40" />
            <StackLayout>
              <Label Text="{Binding Title}" />
              <Label Text="{Binding Details}" FontSize="Micro" />
            </StackLayout>
          </StackLayout>
        </ViewCell>
      </DataTemplate>
    </ListView.ItemTemplate>
  </ListView>
</ContentPage>