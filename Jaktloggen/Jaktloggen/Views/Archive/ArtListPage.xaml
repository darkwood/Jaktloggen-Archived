<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:abstractions="clr-namespace:ImageCircle.Forms.Plugin.Abstractions;assembly=ImageCircle.Forms.Plugin.Abstractions"
             x:Class="Jaktloggen.Views.ArtListPage">
  
  <ListView ItemsSource="{ Binding GroupedItems }"
          IsGroupingEnabled="true"
          GroupDisplayBinding="{ Binding Name }"
          GroupShortNameBinding ="{ Binding ShortName }"
          ItemSelected="ListViewItems_OnItemSelected">
    <ListView.ItemTemplate>
      <DataTemplate>
        <ViewCell>
          <StackLayout Orientation="Horizontal" Padding="5">
            <Image Source="{Binding IconSource}" />
            <Label Text="{Binding Navn}" VerticalTextAlignment="Center" />
            <abstractions:CircleImage Source="{Binding Image}" BorderThickness="0" BorderColor="White" HeightRequest="40" WidthRequest="40" HorizontalOptions="EndAndExpand" />
          </StackLayout>
        </ViewCell>
      </DataTemplate>
    </ListView.ItemTemplate>
  </ListView>
</ContentPage>