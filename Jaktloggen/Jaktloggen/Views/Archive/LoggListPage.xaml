<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:abstractions="clr-namespace:ImageCircle.Forms.Plugin.Abstractions;assembly=ImageCircle.Forms.Plugin.Abstractions"
             x:Class="Jaktloggen.Views.LoggListPage">

    <ListView x:Name="ListViewItems" ItemsSource="{Binding ItemCollection}" ItemSelected="ListViewItems_OnItemSelected">
      <ListView.ItemTemplate>
        <DataTemplate>
          <ViewCell>
            <StackLayout Orientation="Horizontal" Padding="5">
              <abstractions:CircleImage Source="{Binding Image}" BorderThickness="1" BorderColor="White" HeightRequest="40" WidthRequest="40" />
              <StackLayout>
                <Label Text="{Binding FangstText}" />
                <Label Text="{Binding Details}" FontSize="Micro" />
              </StackLayout>
            </StackLayout>
          </ViewCell>
        </DataTemplate>
      </ListView.ItemTemplate>
    </ListView>

</ContentPage>